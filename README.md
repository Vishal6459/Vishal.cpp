# Library Management System 📚

A lightweight, console-based Library Management System built in C++. This application demonstrates fundamental Object-Oriented Programming (OOP) concepts and utilizes file handling for persistent data storage, allowing users to manage a local catalog of books efficiently.

## 🚀 Features

* **Add New Books:** Easily input book details including a unique Book ID, Title, and Author Name.
* **Persistent Storage:** Utilizes C++ File I/O (`<fstream>`) to automatically save all records to a local `library.txt` file, ensuring data is not lost when the program closes.
* **View Inventory:** Read and display all stored books from the file in a clean, formatted layout.
* **Search Functionality:** Quickly search for a specific book by its title using string comparison techniques.
* **Interactive Console UI:** A simple, menu-driven interface that keeps the application running until the user chooses to exit.

## 💻 Tech Stack

* **Language:** C++
* **Libraries:** `<iostream>`, `<fstream>` (File I/O), `<cstring>`
* **Core Concepts:** Object-Oriented Programming (Classes, Methods), File Handling, String Manipulation

## 📋 Prerequisites

To compile and run this project locally, ensure you have the following installed:
* A C++ compiler (such as GCC/G++ or MinGW for Windows)
* An IDE or text editor (like Visual Studio Code, Code::Blocks, or CLion)

## 🛠️ How to Run

1. Clone the repository:
   ```bash
   git clone [https://github.com/Vishal6459/Vishal.cpp.git](https://github.com/Vishal6459/Vishal.cpp.git)
